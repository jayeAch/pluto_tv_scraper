# Clone your repo if you haven't
git clone https://github.com/jayeAch/pluto_tv_scraper.git
cd pluto_tv_scraper

# Initialize Node.js project (creates package.json)
npm init -y

# Install required dependencies (add whatever packages you need)
npm install axios cheerio

# Commit these files
git add package.json package-lock.json
git commit -m "Initialize Node.js project"
git push origin main
